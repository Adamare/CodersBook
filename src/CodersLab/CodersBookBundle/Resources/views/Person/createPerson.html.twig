<!DOCTYPE html>
<html>
    <body>
        {% if error is defined%}
            {{error}} <a href="{{ path ('person_admin_new') }}">Powrót</a>
        {% else %}
            {{ person.name }} <a href="{{ path ('person_admin_all',{ 'id': person.clGroup.id}) }}">Powrót</a>
        {% endif %}
    </body>
</html